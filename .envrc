use flake

# Absolute path for VS Code's `cargo` task type: rust-analyzer launches
# $CARGO verbatim, but a bare `cargo` found on PATH gets resolved by VS
# Code's terminal host, which doesn't see the direnv environment.
# (`type -P`, not `command -v`: the latter returns the bare name here)
export CARGO=$(type -P cargo)
